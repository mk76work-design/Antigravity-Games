# 📚 LESSONS_LEARNED.md

> このファイルはACEフレームワークのCurator役割により管理される。
> 開発中に発見したバグ、設計上の問題、回避策を蓄積し、AIエージェントの推論精度を向上させる。

---

## ⚠️ 更新ルール（Delta Updates 厳守）

- ✅ 新しい教訓は **末尾に追記** する
- ✅ 既存の教訓は **部分修正のみ** 許可
- ❌ このファイルを **全体的に書き直してはならない**
- ❌ 過去の教訓を **省略・要約してはならない**

---

## 教訓一覧

### [2026-08-26] [PixelAnimator] - LLMにピクセルアートを描かせる方式の選定
- **問題**: 「高品質なドット絵アニメーションをAIエージェントに作らせる」という要求に対し、
  画像生成モデル（DALL-E/Stable Diffusion系）を使うか、LLMに直接ピクセルデータを
  生成させるかの選択が必要だった。
- **原因**: 画像生成APIは外部キー・コスト・レート制限が別途必要になり、既存リポジトリの
  「Vanilla JS + Canvas2D」構成との親和性も低い。
- **解決策**: Claude の tool use（構造化出力）で `{ palette: string[], frames: { pixels: number[][] }[] }`
  を直接生成させ、アプリ側の Canvas でレンダリングする方式を採用した。
  `tool_choice` を forced にすることで、自由テキストではなく必ず指定スキーマの
  JSON が返るようにしている。
- **教訓**: LLMにビットマップそのものを描かせたい場合、画像生成APIを挟まなくても
  「パレットindexの2次元配列」という軽量な構造化出力で十分実用的な結果が得られる。
  特に小さい解像度（16〜48px四方）のドット絵とは相性が良い。
- **関連ファイル**: `aiClient.js`

### [2026-08-26] [PixelAnimator] - APIキーの扱い方
- **問題**: ブラウザのみで完結するツールにしたいが、Anthropic API キーをどう扱うか。
- **原因**: バックエンドサーバーを持たない設計にしたため、サーバー側でキーを
  秘匿する一般的な方法が使えない。
- **解決策**: ユーザー自身のAPIキーを `localStorage` に保存し、
  `anthropic-dangerous-direct-browser-access: true` ヘッダを付けてブラウザから
  直接 Anthropic API を呼び出す方式にした（個人利用のプロトタイプツールとして許容）。
  設定ダイアログにキーの保存範囲を明記し、他サービスへの送信がないことを明示している。
- **教訓**: 個人用途のクライアントサイドAIツールでは「Bring Your Own Key」+
  direct-browser-access ヘッダが、バックエンドなしで動く現実的な選択肢になる。
  ただしチーム配布・公開ツールにする場合はプロキシサーバー経由に切り替える必要がある。
- **関連ファイル**: `aiClient.js`, `index.html`（設定ダイアログ）

---

### 🔄 引き継ぎ事項 (2026-08-26)
> 実際のAnthropic APIキーでの生成テストは未実施（サンドボックス環境にネットワーク上の
> 制約があり、fetchをモックしたPlaywrightテストで生成〜描画〜再生成〜書き出しの
> フローを検証済み）。本物のキーで一度生成してみて、パレット色数や単一フレーム再生成の
> 一貫性（隣接フレームとの整合）の実際の出力品質を確認するとよい。
